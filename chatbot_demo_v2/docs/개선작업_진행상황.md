# RAG 품질 개선 작업 — 진행 상황

> 착수: 2026-07-27 · 브랜치 `improve/rag-quality` · 기준 태그 `pre-improve-2026-07-27`
> 계획서: `C:\Users\minsoo\.claude\plans\https-smith-langchain-com-public-12bec45-encapsulated-valley.md`
> **다음 세션은 이 문서부터 읽으면 된다.**

## 지금 어디까지 왔나

| 작업 | 상태 |
|---|---|
| **0. 원복 수단 확보** | 0-1·0-2·0-3 완료 / **0-5 복원 리허설 미완** |
| 7. 평가셋 + 자동 평가 하네스 | **← 다음에 할 일** |
| 1. abstain 오탐 제거 | 대기 |
| 5. 지연·계측 | 대기 |
| 2. 근거 선택·컨텍스트 유연화 + 6. CRAG 가드 | 대기 |
| 3. FAQ 의미 매칭 + 되묻기 | 대기 |
| 4. RAG 품질 게이트 | 대기 |
| 8. 색인 재구축 | 대기 |
| 9. 출처 표기·하이라이트 | 대기 |
| 10. 문서화 | 대기 |

## 완료된 것 (작업 0)

**0-1 git 안전망**
- 태그 `pre-improve-2026-07-27` = `c6b1566` (작업 시작 시점)
- 작업 브랜치 `improve/rag-quality` 생성, `main`은 손대지 않음
- 앞으로 작업 1~10을 **각각 개별 커밋**으로 남길 것 (부분 revert 가능하게)

**0-2 git 밖 데이터 스냅샷**
- `_backup/2026-07-27_pre-improve/` 에 `index/`(69MB) · `.env`(유일본) · `data/*.json` 복사
- `.gitignore`에 `_backup/` 추가 — `git check-ignore`로 `.env`가 무시되는 것 확인 완료

**0-3 test_3 원본 기준값** (최후의 복구 원천)
- `test_3/코드/rag3/index` 9파일 69MB
- `test_3/코드/rag3/cache/parsed_v25` 2,886파일 499MB
- 재색인(작업 8) 전후로 이 값과 대조할 것. 상세는 `_backup/RESTORE.md`

## 남은 것 — 다음 세션 시작점

1. **작업 0-5 복원 리허설** (짧음) — `_backup/.../index`를 임시 폴더에 복원해 로드되는지 1회 확인
2. **작업 7 평가셋 + 하네스** ← 본 작업의 실질적 시작.
   - `data/eval_set.json` (30~50문항 골든셋)
   - `scripts/run_eval.py` (v2 그래프 전체를 타는 러너)
   - `scripts/ab_compare.py` (설정 A/B 비교)
   - 계획서의 "작업 7" 절 참조

## 반드시 기억할 것

- **골든셋 없이 검색 파라미터를 바꾸지 말 것.** 착수 전 조사에서 유력해 보이던 개선안 4개를
  리랭커로 실측했는데 **3개가 오히려 악화**시켰다(프리픽스 제거 / 표 행 분할 / 질의 재작성).
  그래서 작업 7이 최우선이다.
- 작업 중 **폐기한 가설과 실측 수치를 그때그때 메모**할 것. 작업 10 보고서의 "조사 과정" 절은
  나중에 재구성할 수 없다.
- 원격 푸시는 사용자 확인 후에만.
